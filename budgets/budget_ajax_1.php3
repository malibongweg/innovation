<?php
defined('_JEXEC') or die('Restricted access');
$dbo =& JFactory::getDBO();
$doc = & JFactory::getDocument();

if (isset($_GET['action'])) {
	if ($_GET['action'] == "update_budget_config"){
		//$sql = sprintf("upadte cput_system_setup set allow_budget=%d, budget_year=%d,budget_cutoff
	}
exit();
}